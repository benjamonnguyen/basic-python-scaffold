from time import time
import asyncio

NON_PREMIUM_DELAY = 2 * 60.0
PREMIUM_DELAY = 20.0


async def update_non_premium_session_timers():
    # start = time()
    # get from db
    # for record in cursor:
    #     await delete_if_inactive(record)
    #     await update(record)
    # end = time() - start
    # if end < NON_PREMIUM_DELAY:
    #     await asyncio.sleep(NON_PREMIUM_DELAY - end)
    return
